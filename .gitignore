# Runtime / generated
/storage/cache/*
!/storage/cache/.gitkeep
/storage/cache-preview/
/storage/*.png
/storage/clicks.log
/storage/.enrich-progress.json

# Pure-PHP project — no Node.js anywhere (preview runs via `php -S bin/dev-router.php`)
node_modules/
package.json
package-lock.json
*.mjs

# Editor / OS / local
.claude/settings.local.json
.DS_Store

# Downloaded Ticketmaster artist/event images (server-local cache, regenerated by bin/fetch-tm-images.php)
/assets/media/
/storage/tm-images.json

# Runtime-generated slug map (flushed per request; sitemap seeds it)
/storage/slugs.json

# Local secrets — recreate on the server after deploy
/src/config.local.php
